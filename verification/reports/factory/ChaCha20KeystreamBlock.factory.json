{
  "block": "ChaCha20KeystreamBlock",
  "grc_block": "",
  "tier": 3,
  "wave": 9,
  "schema_version": 2,
  "started_utc": "2026-08-29T11:33:45Z",
  "ended_utc": "2026-08-29T21:30:00Z",
  "walltime_sec": 42809.1,
  "tokens": {
    "input": 0,
    "output": 2898834,
    "cache_read": 0,
    "total": 2898834
  },
  "turns": 2560,
  "attempts": 13,
  "human_interventions": 1,
  "intervention_reasons": [
    "User scoped pass 8 as the FINAL pass, then move to Poly1305 + LZ4Encoder regardless of outcome. I briefed it as diagnosis-first (resume from pass 7's branch, expose the resolved assembly, fix ONLY if clear and contained, no new fold search). The diagnosis overturned pass 7's root cause. Block is now PARKED by user decision."
  ],
  "prompts": [
    {
      "role": "initial",
      "text": "Observability first: expose resolved assembly + hop counts, diagnose the dead jump, fix only if contained."
    }
  ],
  "verify_passed": false,
  "quarantined": true,
  "quarantine_reason": "PARKED BY USER DECISION after pass 8, the agreed final pass. Main remains at the pass-6 state: 41 cells, all 32 words emitted, ALL SIXTEEN RFC 8439 S2.3.2 state words BIT-EXACT on the real placed+routed+built chip, emitted in LAP-MAJOR (transposed) order. The arithmetic is proven correct on silicon; only word ORDER is wrong, and a caller can transpose sixteen words host-side. The 48-cell reorder re-fold stays on branch worktree-agent-a47d0f221ad26f23d (regression: emits zero words). ROOT CAUSE NOW KNOWN AND PROVEN -- the reorder band is a single-file row carrying two waves in OPPOSITE directions (store spills WEST, release rides EAST); on the fourth drain lap they meet and two abutting cells each hold the word the other must accept. LAYER: block fold, FIXABLE. Two known shapes for the fix: separate the waves in TIME (source the release trigger from the end of the store wave, bufB3, not from drn in parallel) or in SPACE (give the store wave its own row).",
  "commit_sha": "9b12167 (ON BRANCH worktree-agent-a47d0f221ad26f23d, fold NOT landed)",
  "notes": "=== PASS 4 (2026-08-30) -- THE RING RUNS. It was candidate cause #2, five times over. ===\n\nTHE ANSWER TO THE DISPATCH QUESTION: not a missing HALT, not boot ordering. It was INV-48 root cause C -- A FACE THAT MISSES -- in five different disguises, with INV-50 as the accomplice that let all five hide.\n\nWHAT NOW WORKS, ON THE REAL PLACED+ROUTED+BUILT CHIP, from the execution trace and the emitted words: exactly 80 quarter-round invocations through all sixteen reused stages; 19 half-boundary realignments; 37/38/39 realignment spins of rows 1/2/3; all four taps armed; and STATE WORD 0 BIT-EXACT (0xE4E7 0xF110, RFC 8439 S2.3.2), as is most of word 15. A wrong 20-round permutation cannot produce those bytes, so the datapath, the write-back, the fixed tap and the realignment are CONFIRMED ON SILICON.\n\nTHE FIVE DEFECTS. (1) Four cells carried is_face constants naming the wrong direction outright. ONE of them -- wbk's row-trigger face NORTH, off the top of the array -- IS the whole of 'the ring never starts': wbk had ZERO trace events. (2) THE FACE REGISTER PERSISTS ACROSS ENTRIES; two cells flipped and never restored, so the next entry fired in the last one's direction. (3) A CELL'S FLIP ALSO DEFLECTS WORDS THAT MERELY TRANSIT IT: wb sits on seq's walk and left itself pointing north, so every lap-start trigger bounced back into seq -- the ring did exactly ONE lap then ping-ponged forever. (4) An internal edge (in0.trig -> in1.default) was never declared. (5) THE BIG ONE: the router faced tap3 at its ADDER, walked that face, found tap3->add3->out->in0 and sized tap3.q->in0 at THREE hops; the word actually leaves EAST and overshot by two, so EVERY FRAME REACHED THE COLLECTOR TWO WORDS SHORT and the cipher ran five laps and stalled. That single edge was worth 75 of the 80 laps.\n\nTHE MEASUREMENT THAT GENERALISES (new INV-NEXT). Over all 233 internal edges: RESTING-face emits 211 correct / 0 wrong; FLIPPED-face emits 16 correct / 6 WRONG -- and all 16 'correct' ones are right ONLY BY COINCIDENCE (the resting walk missed, Manhattan fired, Manhattan happened to match; every one is a straight-line hop of 1 or 2). That is INV-50's real shape, and the spurious-success case is WORSE than the Manhattan fallback because nothing looks anomalous.\n\nTWO TOOLCHAIN FIXES, BOTH REGRESSION-TESTED AT 1219 WITH ORIENTATION GREEN. router._place_block_cells looked programs up by the raw positional INDEX, False for every STRING-KEYED block -- so such a block never had its declared fwd_face honoured at all; now keyed by position. And _get_routing_distance takes an optional start_face, with blocks able to declare emit_faces() -> {(cell,port): neighbour_id}. The value is a CELL ID, not a direction, so the face is derived from PLACED coordinates and the declaration is orientation-correct BY CONSTRUCTION -- which is precisely what the previous by-hand-rotation attempt got wrong.\n\nMETHOD, building on the last pass's lesson. 'A model that flatters the design is worse than no model' needed a second turn of the screw: reading MOVE [FACE] out of the real programs is necessary but NOT sufficient. The checker must symbolically execute every path over both sides of every branch and iterate the face register TO A FIXPOINT -- seed with the resting face, collect what each path can LEAVE BEHIND, re-seed, repeat. A checker that assumes each entry starts clean cannot see defects 2 or 3, and those were half the bugs.\n\nWHAT REMAINS: four words. The drain must run four laps with a plain rotate between them; the rotate must come from the only cell reaching all four rows on one walk (wbk at (1,0)) and the lap must be closed by one of the only two cells that can reach wbk, because the tap line and the finish row run ONE-WAY AWAY from the control corner (measured, all four faces, every tap/adder/egress). Cost 5+3+6 words; available 4 short even after compressing wbk's realignment 12 jumps -> 8.\n\nTRAP FOR THE NEXT PASS, which cost the last hour: freeing one of those words by dropping seq's redundant 'MOVE half, four' FITS AND BREAKS THE BLOCK. Shortening seq moves seq.step's entry from 15 to 14 and the build then mis-resolves wbk.back to it instead of row0.pub (also 15) -- the realignment ran perfectly and handed control to the lap counter, and the ring died at the first boundary with a flawless trace. Entry addresses are params-dependent (INV-6/11) and that reaches INTERNAL edges: re-read the BUILT words, not just the budget.\n\nHONESTY NOTE: still not done, and deliberately not marked so. It emits a quarter of its output. But the remaining defect is now a counted word budget on a named cell trio, not 'it does not compute'.\n\nTHE HEADLINE: THE SELECTOR WAS UNNECESSARY. The previous pass's architecture (8 lane cells, LOAD-indirect read, 4-way CMP/BR write-back, fan-out-8 selector broadcast, 8-cell demux) is replaced by a FIXED-TAP RING. Reading the schedule as a per-row READ OFFSET rather than as index(k)=4k+((j+k*shift)&3) collapses it: row k reads offsets 0,1,2,3 in the column half and k,k+1,k+2,k+3 in the diagonal half, so EVERY row taps SLOT 0 provided it rotates left by one after each quarter round, and the diagonal half is the same walk started k positions later (bracketed by k extra rotations and 4-k to restore alignment; a 4-slot rotation has order 4 so the brackets cancel). The tap is a CONSTANT and the whole permutation is a SHIFT REGISTER.\n\nProven EXACT against RFC 8439 S2.3.2 (state + the 64 keystream bytes) and S2.4.2 (the full 114-byte two-block encryption vector) over the same cell-level operations the hardware performs, with 8 INV-4 mutants. FOUR of those mutants -- diagonal-half-first, no-realignment, reversed spin direction, stuck tap -- ALL still perform exactly 80 invocations, so no count-based or structural check catches any of them. Same class as the counter-direction mutant found in attempt 2.\n\nMEASURED ON THE REAL CHIP: (1) the row cell -- publish the head pair, install a written-back replacement, rotate four 32-bit slots -- correct on silicon, 4 spins the identity, 16 instructions with 5 spare words; (2) INV-48's forwarding rule watched LIVE as a failure and then a fix (a cell whose resting face misses its target gives NO output and NO error; an in-program FACE flip fixes it); (3) a word TRANSITS cells carrying a FACE and NO PROGRAM at distances 2/3/4/6 -- but the build faces a bare array cell only where a ROUTE claims it, so a gap inside a block's OWN footprint is still a dead end for a block-internal WRITE.\n\nNEW SILENT TRAP FOUND AND GATED -- POSITIONAL PAIRING (INV-33/INV-50): build_cell_programs and default_layout must ITERATE IN THE SAME ORDER, because the router and build walk the programs and the placed cells in lockstep BY POSITION. Both dicts are keyed by cell id, which HIDES the mismatch: the design places, routes, builds and DRCs clean while whole cells come out with EMPTY memory. Cost one debug cycle here.\n\nNEW INV-50 (three mechanisms, all silent): a CLOSED RING TRAPS ITS INTERIOR (every ring cell forwards along the ring, so nothing emitted inside can ever leave -- measured over all 26 rotations and four emit directions; fold as a serpentine, not a ring, whenever an interior cell must reach an edge); the positional-pairing contract; and the gap/transit asymmetry above.\n\nMETHOD NOTE: the fold-search checker initially credited a FACE flip to any cell I nominated, and passed a layout that then mis-resolved on silicon -- a flip only exists if the cell's PROGRAM contains MOVE [FACE]. Rewriting the checker to read the programs turned three 'green' edges red immediately. A model that flatters the design is worse than no model.\n\n=== PASS 3 (2026-08-29/30) \u2014 architecture REPLACED and simplified ===\n\nTHE PRIOR PASS'S PLAN WAS UNNECESSARY. It had 8 lane cells, LOAD-indirect reads, a 4-way CMP/BR write-back, a fan-out-8 selector broadcast and an 8-cell demux - 37 cells and 36 hard edges. Reading RFC 8439's schedule as a per-row READ OFFSET instead of the closed form index(k) = 4k + ((j + k*shift) & 3) collapses all of it: row k reads offsets 0,1,2,3 in the column half and k,k+1,k+2,k+3 in the diagonal half, so EVERY ROW TAPS SLOT 0 if it rotates left by one after each quarter round, and the diagonal half is the same walk started k later. The tap becomes a CONSTANT and the permutation becomes a SHIFT REGISTER. 40 cells / 58 plain edges replace 37 cells / 36 hard ones. Proven exact against RFC 8439 S2.3.2 (state AND keystream bytes) and S2.4.2 (the full 114-byte two-block vector) with 8 INV-4 mutants - four of which (diagonal-first, no-realign, reversed spin, stuck tap) still perform exactly 80 invocations, so ONLY A VALUE GATE CATCHES THEM.\n\nMEASURED ON REAL SILICON: the row cell publishing, installing a write-back and rotating (4 spins = identity); INV-48's forwarding rule observed LIVE as a failure then a fix - a cell whose resting face misses its target gives no output AND NO ERROR; and a word transiting cells that have a face but no program at distances 2/3/4/6 - though the build faces bare cells only where a ROUTE claims them, so a gap inside a block's own footprint is still a dead end for an internal WRITE.\n\nNEW INV-51 (renumbered from the builder's INV-50 - third numbering collision this campaign; parallel builders cannot see each other's KB additions): (1) A CLOSED RING TRAPS ITS INTERIOR - nothing emitted inside a ring fold can ever leave, so fold as a serpentine when an interior cell must reach an edge. (2) POSITIONAL PAIRING - build_cell_programs and default_layout must iterate in the SAME ORDER; the cell-id keys HIDE a mismatch and leave whole cells with empty memory. (3) the gap/transit asymmetry above.\n\nIT COMMITTED THE SOURCE THIS TIME, unlike pass 2, and the reasoning is sound: the block builds and is statically green, so the next agent inherits a working toolchain rather than 37 loose cells. It is explicitly marked non-computing in the docstring, the manifest and the lessons log.\n\nTHE LESSON IT VOLUNTEERED, which is the best thing in this report: its first fold-search checker credited face flips to cells it had merely NOMINATED, and passed a layout that mis-resolved on silicon. Rewriting it to read MOVE [FACE] out of the actual programs turned three 'green' edges red immediately. 'A MODEL THAT FLATTERS THE DESIGN IS WORSE THAN NO MODEL.' That is the same failure class as the fabricated KB limits this campaign spent a day removing.\n\nORCHESTRATOR VERIFICATION: 151 tests pass from main across the keystream golden, the new fixed-tap-ring suite, the QR block and the transit ceiling; manifest honestly needs_human; dashboard clean.\n\n=== PASS 4 (2026-08-30) \u2014 THE RING RUNS; INV-50 CLOSED ===\n\nIT WAS CANDIDATE #2 - A FACE THAT MISSES (INV-48 root cause C) - FIVE SEPARATE TIMES. Not a missing HALT, not boot ordering. INV-50 was the accomplice that let all five hide:\n  (1) Four cells named the wrong direction outright. One - wbk's row-trigger face NORTH, off the top of the array - WAS the entire 'ring never starts' symptom: wbk had ZERO trace events because wb's jump left on a face with no cell on it.\n  (2) THE FACE REGISTER PERSISTS ACROSS ENTRIES. wbk and seq each flipped for one burst and never restored, so the next entry fired in the previous entry's direction.\n  (3) A CELL'S FLIP ALSO DEFLECTS WORDS THAT MERELY TRANSIT IT. wb sits on seq's walk and left itself pointing north, so every lap-start trigger bounced back into seq, which re-entered step and ping-ponged. The ring did exactly ONE lap then oscillated forever.\n  (4) in0.trig -> in1.default was never declared in internal_jumps at all.\n  (5) The router faced tap3 at its adder and sized tap3.q -> in0 at THREE hops instead of one. Every frame reached the collector two words short. THAT ONE EDGE WAS WORTH 75 OF THE 80 LAPS.\n\nPROVEN ON THE REAL PLACED+ROUTED+BUILT CHIP: 80 quarter-round invocations through all sixteen stages, 19 half-boundary realignments, 37/38/39 realignment spins of rows 1/2/3, all four taps armed, and STATE WORD 0 BIT-EXACT AT 0xE4E7F110 - the RFC 8439 S2.3.2 first output word. That is a VALUE gate, not a counting one: four of this cipher's mutants (diagonal-first, no-realign, reversed spin, stuck tap) all still perform exactly 80 invocations.\n\nTHE GENERALISABLE MEASUREMENT (now INV-52): over all 233 internal edges, resting-face emits are 211 correct / 0 wrong, but flipped-face emits are 16 correct / 6 wrong - and ALL 16 'correct' are right ONLY BY COINCIDENCE (the resting walk missed, Manhattan fired, and Manhattan happened to match; every one a straight-line hop of 1-2). A resting walk that succeeds SPURIOUSLY is worse than INV-50's fallback because nothing looks anomalous.\n\nINV-50 IS CLOSED, and better than the fix I proposed. I had said to rotate faces by hand; it instead had blocks declare emit_faces() returning a CELL ID, not a direction, so the face is derived from placed coordinates and is orientation-correct BY CONSTRUCTION - which is exactly what the earlier by-hand-rotation attempt got wrong. It also found router._place_block_cells looked programs up by raw positional INDEX, false for every string-keyed block, so those never had their declared fwd_face honoured at all. placekyt/tests 1219 passed (baseline), orientation suite green.\n\nTRAP RECORDED FOR THE NEXT PASS: freeing a word by dropping seq's redundant MOVE half,four FITS AND BREAKS THE BLOCK - shortening seq moves seq.step's entry 15->14 and the build then mis-resolves wbk.back to it instead of row0.pub (also 15). Entry addresses are params-dependent (INV-6/11) and that reaches INTERNAL edges.\n\nMETHOD NOTE, sharpening the previous pass's: 'a model that flatters the design is worse than no model' needed a second turn. Reading MOVE [FACE] from real programs is NECESSARY BUT NOT SUFFICIENT - the checker must symbolically execute every path over both branch sides and iterate the face register TO A FIXPOINT. A checker assuming each entry starts clean cannot see defects 2 or 3, i.e. half the bugs.\n\nORCHESTRATOR VERIFICATION: 117 tests pass from main; the end-to-end gate genuinely places, auto-routes, builds the bitstream, runs simKYT and reads the trace back (not a proxy); 0xE4E7F110 confirmed against the golden's RFC vector; placekyt/tests 1219 passed with the INV-50 fix applied. INV-52 assigned at landing (the builder correctly left it INV-NEXT).\n\n=== PASS 5 (2026-08-30) \u2014 32 WORDS, ALL SIXTEEN STATE WORDS EXACT ===\n\nADDING A CELL WAS PART OF THE ANSWER, as the brief predicted from LZ4: `drn` at (1,2) resting NORTH, all five of its jumps riding the resting face with no flip; the lap closes tap3 -> (south, hop 19, through FOURTEEN TRANSPARENT STAGES) -> add_pad (26 spare words) -> drn. 41 cells. Also load-bearing and easy to miss: `drn` must be listed BEFORE the rows in program order, since that is what decides backward-vs-forward.\n\nMY BRIEF WAS WRONG ON TWO COUNTS AND THE BUILDER MEASURED BOTH:\n  (1) 'Four words short' was not the real constraint, and 'only wbk at (1,0) reaches all four rows' was DERIVED by the previous pass. Exhaustive over every free slot x every face: NINETEEN slots do. That is the FIFTH time in this campaign a derived 'cannot' has been wrong.\n  (2) TWO THINGS THE PREVIOUS PASS RECORDED AS PROVEN WERE THE TWO REMAINING BUGS, both sitting inside assertions that PASSED:\n      - THE 20th REALIGNMENT WAS MISSING. Spins were 37/38/39 where the schedule needs 40/40/40 - exactly 10a+9b against 10a+10b. The old gate asserted 19 boundaries and 37/38/39 AS THE REQUIRED COUNTS. It hid because row 0's bracket is zero spins, so row 0 - the RFC's first output word, and the only one the old gate checked - was bit-exact while rows 1-3 drained the wrong slot. A word-0 gate structurally COULD NOT see it. Now INV-54.\n      - A BACKWARD JUMP HAD BEEN SILENTLY REDIRECTED FOR A WHOLE PASS. The build rewrites the source cell's HIGHEST-ADDRESSED jump, not the named one; wbk.back was being repointed at seq.step. It 'ran' only because seq.step and row0.pub both resolved to address 15. This is the trap my brief described - but from the OTHER side: the edge was already broken and the collision was MASKING it, so the pass that 'freed a word' did not break the block, it EXPOSED a break. Now INV-53.\n\nTHE LESSON: a value gate on ONE word is not a value gate. The previous pass's 0xE4E7F110 check was a genuine improvement over counting - and still could not see a defect that left rows 1-3 draining the wrong slot. The gate now asserts all sixteen words and was PROVEN to fail when perturbed; the fold gate now runs with ZERO exemptions.\n\n28 gates (3 new: the 19-vs-20 INV-4 negative, backward-jump-per-cell + highest-addressed, and seq.step != row0.pub). 1090 tests pass across chacha/saturation/chip-scale/GRC-binding/orientation. NO engine/ or router.py changes, so the 1219 and 364 baselines are untouched.\n\nENVIRONMENT NOTE WORTH PINNING: no interpreter in the worktree could run the suite - the simkyt .so is cpython-3.12 while the default python is 3.13. The builder used the anaconda dsp_sw_radio env and had to pip install pytest/PySide6/ruamel.yaml plus set QT_QPA_PLATFORM=offscreen and the PySide6 plugin path.\n\nORCHESTRATOR VERIFICATION: 51 chacha tests pass in-worktree, 115 with LZ4 from main; the all-sixteen-words assertion and the 19->20 / 37-38-39 -> 40/40/40 correction confirmed in the gate source; both new bug gates confirmed present. INV-53 and INV-54 assigned at landing (the builder correctly left them INV-NEXT).\n\n=== PASS 6 (2026-08-29) \u2014 TWO AVENUES CLOSED BY PROOF, FIX LOCATED ===\n\nTHE BOOT-TIME PERMUTATION IS DEAD, AND BY A REAL PROOF RATHER THAN A BUDGET. This was the cheapest conceivable fix (different initial_value constants = zero instructions, zero cells) so it was tested first. Row k slot i is read on exactly the laps where that row's rotation offset is i, and the quarter round demands a SPECIFIC RFC index there; walking the 80 laps PINS all sixteen (row, slot) cells with NO conflict and NONE left free. The unique solution is the identity the block already ships. The gate derives the map from the QR schedule and was mutation-proven: perturbing the expected identity by one slot fails it. My brief had said pass 5's 'relabelling changes values not order' argument was about ADDERS and should not be inherited for a BOOT-TIME permutation -- that was right, and the conclusion survives anyway by a different and stronger route.\n\nROW-SIDE LOOPS ARE DEAD AND NOW UNDERSTOOD STRUCTURALLY, not as a budget miss. Exhaustive over 4^4 x 4^4 x 4! drain knobs: none works, best 4/16. The reason: ONE DRAIN LAP VISITS EACH ROW ONCE, so the row index is the FAST-VARYING half of the output position, and no permutation of laps or rows can exchange the nibbles. That is a far better statement than pass 5's version.\n\nTHE COLLECTOR AVENUE WAS CORRECT. The word wanted at output position 4k+L is the one add_k produces on lap L, so output group k IS add_k's four words in lap order. 'Minimum buffering depth' was the right question to force: a SINGLE streaming reorderer needs 9 words held, but SPLIT PER ADDER it is 4 each. Simulated at instruction level it emits S2.3.2 order exactly.\n\nTHE SEALED-ROW FINDING generalises pass 5's result. Pass 5 reported 'd3 has ZERO candidates' and blamed tap3's position; the real cause is that THE WHOLE FINISH ROW IS ENCLOSED -- no cell on it reaches any free slot on any face (north leaves the block, south is swept away by the state line's uniform EAST facing, east/west stay on the row). A true observation with the wrong attribution is still a poisoned one; this is the corrected form. Now INV-55 (assigned at landing; the builder correctly wrote INV-NEXT).\n\nENVIRONMENT TRAP, NOW PINNED IN THE KB AND VERIFIED BY ME: inside a worktree the venv resolves gr_kyttar/simkyt to the MAIN checkout, so worktree edits silently no-op AND TESTS FALSELY PASS. Fix: PYTHONPATH=<worktree>/runtime/python, confirmed via module.__file__. This is the same shape as the stale-simkyt.pth trap already in memory, and is likely part of what cost pass 5 time.\n\nINCIDENTAL, KEEP FOR THE RE-FOLD: splitting the adder's output into oh/ol with ONE trailing trigger costs nothing (a WRITE's target is an instruction field) and SAVES an instruction.\n\n6 new gates, EACH mutation-proven to fail (INV-4). The buffer mutant reproduces the block's ACTUAL defect [0,4,8,12,...], so it discriminates the real bug rather than a proxy. 1244 passed / 0 failed. Fold gate zero exemptions. No engine/ or router.py, so 1219/364 hold.\n\nORCHESTRATOR VERIFICATION: 57 chacha tests pass in-worktree (was 51), 121 with LZ4 on main; engine/router diff vs main is EMPTY; uniqueness gate mutation-tested by me and it failed as required; worktree module resolution confirmed by __file__.\n\n=== PASS 7 (2026-08-29) \u2014 RE-FOLD BUILT, RELEASE PATH DEAD, NOT LANDED ===\n\nDECISION: I did NOT land this. Main stays at the pass-6 state (32 words, all sixteen values bit-exact, transposed). Trading 32 correct words for 0 is a regression however good the reasoning behind it, and the fold rewrite is 794 lines. The branch `worktree-agent-ae5f3af0178f450a5` is preserved intact so pass 8 resumes from it rather than rebuilding. Metrics and KB land; block code does not.\n\nVERIFIED BY ME: the cipher is genuinely intact behind one broken edge. The gate fails at `runs.get(('out','default')) == 32` -> None, while every upstream count passes: add0..add3 fire 4x each, drn 4 laps, all taps armed, the 80-lap compute schedule green. That is exactly the builder's characterisation, and it is the difference between 'broken block' and 'good block, dead release path'.\n\nTHREE MEASURED RESULTS THAT GENERALISE (the real value of this pass):\n  1. DEPTH-2 IS MUCH CHEAPER THAN HALF OF DEPTH-4: 22 instructions / base_addr 9 / 8 live, vs the depth-4 form's 20/11/13. A two-slot cell emits BOTH words from one straight-line entry, which deletes the counter, the re-entry AND the westward hop. Pass 6 predicted 'frees four registers' and UNDERSTATED it.\n  2. FIFO ORDER IS PASS ORDER: the reorder needs no schedule at all; only the stages' ORDER ALONG THE CONVEYOR has to be arranged.\n  3. A UNIFORMLY-FACED LINE IS A WALL FROM BELOW -- the converse of INV-55 rule 2. Nothing in the control corner can climb through the state line; only cells already owning an off-axis flip (the four taps) can lift a word off it. So the chain head was placed where tap0's existing inward walk already lands, at ZERO new face constants.\n\nDEAD ENDS, ALL MEASURED: two relay routes (`wb` at 22 instructions with faces pinned at R8/R9 behind the frame; `wbk` with 4 spare where a third face plus a flip pair needs 4), and a spill-after-shift reordering that would have saved a word but CORRUPTS THE FIFO -- it emits 1,2,2,3,5,6,6,7,... The builder caught that by SIMULATING rather than reasoning, which is the campaign rule working as intended.\n\nENVIRONMENT: the builder cleared the worktree-resolution trap BY MUTATION rather than inspection -- changed drn's lap count 4->3, watched the on-chip gate fail, restored. Also found the tests derive sys.path from __file__, so they are worktree-local regardless of PYTHONPATH. Stronger than the pass-6 note.\n\nNEXT PASS'S FIRST MOVE, per the builder and I agree: expose the RESOLVED ASSEMBLY AND HOP COUNTS from the built bitstream. BuildResult does not surface them, and that missing observability is what blocked closure -- four candidate causes were eliminated and the fifth could not be SEEN. Note this may need a read-only accessor; if it touches placekyt/engine/ it must be additive and the 1219/364 baselines re-run.\n\n=== PASS 8 (2026-08-29) \u2014 PASS 7'S ROOT CAUSE WAS WRONG; IT DEADLOCKS ===\n\nTHE HEADLINE: there is no dead jump. chip.run() returns stop_reason == 'Deadlock'. Neither pass 7 nor the invariant it wrote ever read that field -- and a deadlock and a clean run-to-quiescence look IDENTICAL in the trace and in the emitted-word count. Pass 7 eliminated four candidate causes correctly and then spent the rest of its budget on a fifth that did not exist. THE LEAD RULE OF THE NEW INVARIANT IS THEREFORE: READ stop_reason FIRST.\n\nBoth release jumps resolve CORRECTLY, read straight out of the built bitstream:\n  bufB0.rel: [29] 0x72f3 JUMP @8 entry=19 -> out       (19 IS out's entry addr)\n             [30] 0x73d5 JUMP @1 entry=21 -> bufA0.rel (21 IS bufA0's rel addr)\nand the words physically leave: the first release pair is 0xe4e7/0xf110 = 0xE4E7F110, RFC 8439 word 0, correct.\n\nTHE REAL MECHANISM: the reorder band is ONE eastward single-file row carrying TWO WAVES IN OPPOSITE DIRECTIONS. The STORE wave spills WEST (each A stage into its own B stage, once per drain lap); the RELEASE wave rides EAST to the egress. On the fourth drain lap they overlap and two abutting cells each hold the word the other must accept:\n  bufA3 (9,0) output_ready face=W -> neighbor 8 (bufB3)   # store spill, westward\n  bufB3 (8,0) output_ready face=E -> neighbor 9 (bufA3)   # release word, eastward\nwith the whole row queued behind and `out` never executing. TWO INDEPENDENT CONFIRMATIONS: the store-count signature is every stage 4 but bufB3 THREE -- the one spill that never landed; and suppressing ONLY the release trigger flips stop_reason to QueueEmpty. Margin is 83 ns (drn fires the release that soon after bufA3's last store) against a ~207 ns spill.\n\nCOMPOUNDING IT: a head-on RESTING-FACE pair, `out` NORTH at (9,1) vs `bufA3` SOUTH at (9,0), forced because the chip's x16_out port cell IS (9,0), so the router brings the egress net north into a block-owned cell (blk_out route = [(9,1),(9,0)]).\n\nMY OBSERVABILITY BRIEF WAS BASED ON A FALSE PREMISE AND THE BUILDER CORRECTED IT: NO engine change was needed. BuildResult.chips[N].cells ALREADY exposes every cell's resolved 32-word memory, entry address and face. Pass 7's 'BuildResult does not expose them' was simply wrong, and I repeated it in the brief. A ~15-line TEST-SIDE disassembler printed the whole band. Zero files under placekyt/engine/ touched.\n\nTHE REUSABLE ARTEFACT: test_no_two_block_cells_rest_facing_each_other finds the pair STATICALLY from _geometry() alone -- no simulation. The predicate _resting_face_conflicts() takes any block's geometry, so it generalises past this block. Nothing in place, route, build or DRC reports this class, and on chip it presents as 'the output cell never runs', which reads exactly like a mis-resolved jump. That is what cost pass 7. Now INV-56.\n\nFive candidate fixes measured dead: all four `out` faces; removing bufA3's spill; band shifted west (overlap DRC); bufA3 resting EAST (bufA0.o0h -> out.v0h undeliverable). Per scope the builder did NOT start a new fold search.\n\nGATES: placekyt 1219 passed, EXACT baseline. GRC-binding + orientation 902. placement-legality + saturation 226. reachability + chip-scale 24. Environment verified by MUTATION (drn lap 4->3 changed on-chip spin counts, then restored).\n\nLANDED ON MAIN: the KB correction (pass 7's claim marked corrected IN PLACE, not deleted, per the evidence rule), the lessons entry, and INV-56. NOT landed: the 48-cell fold.",
  "model": "claude-opus-5",
  "by_model": [
    {
      "model": "claude-opus-5",
      "attempts": 13,
      "turns": 2560,
      "walltime_sec": 42809.1,
      "output_tokens": 2898834
    }
  ]
}
