{
  "block": "Poly1305MACBlock",
  "grc_block": "",
  "tier": 3,
  "wave": 3,
  "schema_version": 2,
  "started_utc": "2026-08-29T22:10:00Z",
  "ended_utc": "2026-08-30T22:10:00Z",
  "walltime_sec": 6618.6,
  "tokens": {
    "input": 0,
    "output": 1339285,
    "cache_read": 0,
    "total": 1339285
  },
  "turns": 260,
  "attempts": 2,
  "human_interventions": 1,
  "intervention_reasons": [
    "Agent was cut off mid-build by an API weekly limit (not a failure) with three good files UNCOMMITTED. I verified the worktree survived, found the gap that mattered -- 62 golden tests passing but ZERO on-chip execution -- and resumed the SAME agent with its context rather than restarting, telling it to commit first and then build to the on-chip finish line. I also forwarded INV-56 (read stop_reason first), which it then extended."
  ],
  "prompts": [
    {
      "role": "resume",
      "text": "Commit first. Your gates are model-only -- no chip.run anywhere. Build to the on-chip finish line; read stop_reason."
    }
  ],
  "verify_passed": true,
  "quarantined": false,
  "quarantine_reason": "",
  "commit_sha": "db0dab1,82370f3,9da7ad8,9ab5f4f,667ed3f,+final",
  "notes": "=== PASS 1 (2026-08-29/30, interrupted by weekly limit, resumed) ===\n\nMY PLAN SPEC WAS WRONG AND THE BUILDER CAUGHT IT BY MEASURING. S B5 specified FIVE RADIX-2^26 LIMBS, the textbook Poly1305 decomposition. It is NOT IMPLEMENTABLE HERE: MUL/MULHI are SIGNED (measured on chip: 0x0002 x 0xFFFF -> 0xFFFFFFFE), so an exact unsigned 16x16->32 product needs BOTH operands under 2^15. A radix-2^26 limb alone blows that, and radix 2^13 fails too -- the limb fits but the FOLDED COEFFICIENT 5 x 8191 = 0x9FFB does not. RADIX 2^10 / 13 LIMBS is the only survivor keeping 2^130 = 5 limb-aligned (10 x 13 = 130). This signedness was NOWHERE in the knowledge base, which is exactly why my spec got it wrong. Now INV-57, gated in test_poly1305_golden.py, and I have CORRECTED THE PLAN DOC in place with the reasoning rather than leaving a known-wrong spec to guide the next builder.\n\nFOUR MEASURED FINDINGS, all now invariants:\n  INV-57 MUL/MULHI signed -> both operands under 2^15.\n  INV-58 A 32-bit MAC is 7 instructions and must compute the HIGH HALF FIRST -- MULHI destroys the carry, and the wrong order leaves the LOW WORD BIT-EXACT, invisible to a one-word gate. That is the campaign's recurring shape yet again. Also: a systolic stage cannot adopt-and-forward in one entry (wrong in all 6 step orders x 2 trigger orders); sweeps must be staged AND fired in REVERSE ring order (forward left 12 of 13 cells exact).\n  INV-59 A PROGRAMMED cell on a broadcast walk stops the sweep dead; only a face-only transit_* is transparent.\n  INV-60 Read the hop the BUILD resolved. A manhattan hop GUESS instead of the build's input_landings gave 8 events and no state change and looked exactly like a dead block; the bitstream was correct and survived full disassembly. The resolved landing gave 1445 events. A HARNESS bug presenting as a block bug.\n\nIT EXTENDED INV-56 (which I forwarded from ChaCha20): the SAME layout reported Deadlock on some inputs and QueueEmpty on others, so 'read stop_reason' must be 'read it for EVERY case', not once.\n\nTWO MEASURED DEAD ENDS: a dedicated x5 wrap CELL cannot be ordered between sweeps by any trigger placement (the fold must ride the producing cell's forward); and folding the egress onto a MAC cell corrupted ONLY THAT CELL's accumulator -- 12/13 correct, exactly the shape a one-value gate misses.\n\nHONEST INTERFACE LIMIT the builder flagged: the input is a 16-bit WORD stream, so odd-byte messages are not expressible (3 of 9 S A.3 vectors), gated at the golden instead. LAYER: block interface, FIXABLE.\n\nORCHESTRATOR VERIFICATION: I checked whether the on-chip claims were backed by committed code and they are NOT -- those experiments ran in scratch and were not committed, so they are not reproducible from the tree. That is a REPRODUCIBILITY GAP, not fabrication: the committed gate file opens by stating plainly 'this file does NOT gate a built chip', names every unbuilt phase, and keeps the block out of the catalog. The discipline worked. Next pass must COMMIT the on-chip harness, not just run it.\n\nGATES: placekyt 1219 (baseline, zero regressions), orientation 364, binding+placement 652, chacha/lz4/saturation/chip-scale/reachability 339, poly1305 70 -- INV-4 mutations each proven to fail (r clamp, high bit, SEVEN wrong reduction constants, +s, wrong MAC order, one-stage carry). Fold gate zero exemptions. No engine or router changes.\n\n=== PASS 2 (2026-08-30, resumed after an API session limit) === DONE.\n\nTHE RFC 8439 S2.5.2 TAG IS EXACT ON THE REAL PLACED+ROUTED+BUILT CHIP -- the\nfull 8-word tag, 17-word worked example with a partial final block -- plus\nall six even-length S A.3 vectors, a final-block residue sweep m=1..8,\nseeded randoms, the all-max carry ceiling, and the message driven SATURATED\nback-to-back under the INV-20 serialize-LOCK. Seven INV-4 mutants each\nplaced+routed+built+run and each PROVEN able to fire first (the one-stage-\ncarry mutant's stimulus had to be SEARCHED: candidate #143 of 3000 reaches\nthe 17-bit split sum). stop_reason read for EVERY case.\n\nPASS 1'S SCRATCH CLAIMS WERE RE-ESTABLISHED BY REPLACEMENT, NOT TRUST: the\nsystolic-ring-with-fan-sweeps design (whose staging hazards consumed pass 1)\nwas replaced by an ALL-SERIAL conveyor-cycle architecture -- a MAC has no\nthroughput requirement, so every phase is a serial chain and the INV-58\nhazard class cannot occur. 100 cells, CHIP_SCALE 10x10, 94 programs + 6\ntransits: 13 limb groups [mulA lh mulC mulB fin], 8 per-word-slot packers,\na sequencer row, closure return relays, an unlock relay chain. What pass 1\nmeasured that SURVIVED: INV-57's radix (2^10/13 limbs), the INV-58 MAC order\n(now an on-chip mutant), receiver-side x64, the odd-byte interface limit.\n\nNEW MEASURED RULES (INV-NEXT appended): (1) latency-tolerant blocks should\nbe all-serial on one conveyor cycle; (2) a DECLARED backward write delivered\non a FLIP is re-patched to the resting-corridor hop (authored @1 became @21\n-> permanent ping-pong Deadlock) -- author such edges as literals with\nbuild-time-resolved registers and do not declare them; corollary: a face\nconstant that is not is_face=True is invisible to the router's flip-face\nwalk (resolved distance 99, build refused). Also re-measured: INV-61.4 (the\nlanding IS the first program-dict cell), INV-63's egress discipline\n(RAW_OUTPUT_HOPS + literals; the resolved port write was 92 hops otherwise),\nthe hop-31 ceiling (a 32-hop control edge failed the build; the high-bit\ntrigger for m>=4 routes through the unlock relays), MAC-opcode/ADC-capture\nsemantics by probe.\n\nGATES: test_poly1305_mac.py 42 on-chip + test_poly1305_golden.py 70 model;\nplacekyt 1219, orientation 364, binding/saturation/legality/chip-scale green;\nfold gate zero exemptions; NO placekyt/engine or router.py changes. Manifest\ndone; queue 120/120 -- THE QUEUE IS COMPLETE.\n\nORCHESTRATOR VERIFICATION AT LANDING (2026-08-30): 120 poly tests reproduced (50 on-chip/static + 70 golden); my own chip-side mutation (DataWord five 5->3, model untouched, __pycache__ cleared both sides) failed the S2.5.2 tag gate and the restore went back to 50/50 green. The builder flagged rather than silenced two hand-rolled report writers from the OTHER blocks' landings (INV-38 guard) \u2014 fixed at landing by routing both through write_report with MEASURED verdicts (commit c3411f4). Fable token/turn/walltime figures corrected from harness accounting (the builder cannot see its own usage): 783,206 output tokens, 113 tool uses, 78 min. engine/router untouched. Queue: 120/120 done.",
  "model": "claude-fable-5",
  "by_model": [
    {
      "model": "claude-opus-5",
      "attempts": 1,
      "turns": 147,
      "walltime_sec": 1916.0,
      "output_tokens": 556079
    },
    {
      "model": "claude-fable-5",
      "attempts": 1,
      "turns": 113,
      "walltime_sec": 4702.6,
      "output_tokens": 783206
    }
  ]
}
